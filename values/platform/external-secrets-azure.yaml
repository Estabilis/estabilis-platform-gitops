# Azure-specific values for External Secrets Operator (platform)
# Loaded in addition to external-secrets.yaml when provider=azure

serviceAccount:
  annotations:
    azure.workload.identity/use: "true"
    azure.workload.identity/client-id: ""  # injected via ArgoCD helm.parameters

podLabels:
  azure.workload.identity/use: "true"
