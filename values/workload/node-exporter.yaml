# Workload overlay — node-exporter upstream chart values for workload clusters
commonLabels:
  estabilis.io/managed-by: workload-bootstrap
  estabilis.io/component: node-exporter

resources:
  requests:
    cpu: 10m
    memory: 32Mi
  limits:
    cpu: 100m
    memory: 64Mi
