apiVersion: v2
name: workload-bootstrap
description: |
  ApplicationSet that bootstraps the Estabilis baseline on every
  workload cluster registered by the estabilis-workload-operator.
  Rendered by the hub's ArgoCD. See ADR 0001.
type: application
version: 0.2.0
appVersion: "0.2.0"
