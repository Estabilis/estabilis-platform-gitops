# Workload overlay for cert-manager
# Note: cert-manager upstream chart has strict values schema that does
# not allow commonLabels at the top level. Labels are set via the
# platform's values overlay instead.
installCRDs: true
